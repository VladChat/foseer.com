Patch purpose:
* split cache into runtime cache (qwen-cache) and versioned seed cache (qwen-data/cache-seed) [APPLIED]
* add seed-cache fallback in cache readers [APPLIED]
* add seed-cache builder script [APPLIED]
* harden .gitignore for secrets/runtime artifacts while keeping package-lock.json versionable [APPLIED]

Apply by extracting over repo root.
After extract:
1) git status
2) npm run qwen:build-seed-cache   (optional, refresh seed snapshots from current runtime cache)
3) commit and push
