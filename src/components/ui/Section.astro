---
// File: src/components/ui/Section.astro
// Purpose: Reusable layout section wrapper with consistent spacing for Foseer.

interface Props {
  className?: string;
}

const { className = '' } = Astro.props;
---

<section class={`py-16 sm:py-20 ${className}`}>
  <slot />
</section>
